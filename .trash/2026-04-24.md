
## 19:20 修复 Codex GPT-5.5 fallback 排查
- 症状：Dashboard 手选 `openai-codex/gpt-5.5` 后实际 fallback 到 `zenmux-key2/anthropic/claude-opus-4.7`，错误 `model not found`。
- 误判纠正：不是缺 OAuth，也不是简单缺 `codex` plugin allow；`auth-profiles.json` 内 `openai-codex` OAuth 完好。
- 变更：备份 `/root/.openclaw/openclaw.json` → `.bak.20260424-1917`；将 provider 块统一为 `models.providers.openai-codex` 并声明 `gpt-5.5`。
- Gateway：配置 hot reload + 后续重启，当前 pid 547883，probe ok。
- 验证：`session_status model=openai-codex/gpt-5.5` 已不再显示 fallback；后续如再失败，优先核对 OpenClaw 源码对 `gpt-5.5` forward-compat 支持，而不是 auth。
- 19:24 端到端验证：spawn 轻量 subagent 指定 `model=openai-codex/gpt-5.5`，返回 `CODEX55_OK`；`session_status` 显示 latest succeeded 且无 fallback 行，说明 Codex 5.5 已接通。
