# 2026-04-25

## 01:01 mangba 4/24 投递失败复盘（含自我纠错）

**Bruce 诉求**：排查 mangba 4/24 晚 20:00 学习提醒 + 22:00 芒巴晚报都没收到的原因。

### 调查时间线
- 00:32-00:40 第一版结论：归因于 19:17 起 hot-reload 触发的 `openclaw-weixin` crash-restart loop 覆盖了 20:00 和 22:00 —— Bruce 指出 22:00 没有 reload 干扰，否决
- 00:40-00:55 第二版结论（tasks/mangba-delivery-20260424.md）：20:00 = channel crash-loop；22:00 = pid 550041 进入"活着但 cron/delivery 子系统半死"状态（22:18/22:48 "cron service unavailable" 告警）；cron `delivered=true` 是可观测性误报
- 00:55 Bruce 提供**关键对照组**：4/22、4/23 晚报 + 学习**都正常收到**
- 01:00 重新核验 4/22 日志：**4/22 的 cron 主动推送在日志里同样没有 `outbound:` 行**（只有 22:02 Bruce 主动发 inbound 触发的 agent 回复带 outbound log）→ 推翻第二版的"无 outbound 日志=发送失败"前提

### 决定性事实
- cron announce 路径（`fallbackUsed=true` 真实含义 = "cron 用 announce 兜底推送"，不是 channel 降级）**不打 `outbound: text sent OK` 日志**
- 4/22、4/23（成功）和 4/24（失败）在 cron runs jsonl 里的 `status`/`delivered`/`deliveryStatus`/`fallbackUsed` 字段**完全相同**，无法区分
- `mangba` 配置 accountId=`5814497b5df4-im-bot`，这个 bot 在每次 gateway restart 时都会命中 `Cannot read properties of undefined (reading 'logger')` 崩溃；但 4/24 20:06 之后到 22:43 它保持 running
- 4/24 22:18/22:48 两次 `memory-core: managed dreaming cron could not be reconciled (cron service unavailable)` 是**事实**，但我不能推断它就代表 mangba cron delivery 也失效

### 已排除
- stale-cron 跳过（180min 阈值，4/24 延迟 0min，不命中）
- bestEffort 静默失败（配置未开 bestEffort，发送失败会 throw）
- 地址配置错误（to=`o9cq80zPDVZ65x_dc91XzBkQUvr0@im.wechat` 正确，4/22 该地址收到过）

### 诚实的未决状态
**我没有铁证说明 4/24 那两条消息为什么没送到。** 可能的分支：
- A. openclaw 侧 silent fail（需 debug log 才能看到）
- B. ilinkai/`5814497b5df4-im-bot` runtime 静默丢消息
- C. 企业微信/微信客户端侧丢消息

### 给 Bruce 的建议（待确认）
1. 开启 openclaw-weixin 和 run-delivery 的 debug log
2. 今晚手动触发一次 mangba 学习提醒 cron 做端到端测试
3. 若本地确认成功但仍丢消息，怀疑方向转向 ilinkai/微信

### 经验教训（写给未来的自己）
- **"日志里没看到"≠"没发生"**。不同代码路径打的日志 tag 完全不同，必须先读源码确认某个日志点是不是想观察的代码路径必经之路
- Bruce 对技术精度要求高，**宁可说"查不到"也别拿偷懒的因果链蒙**。两次被他戳穿（19:17 hot-reload / 半死状态），要吸取
- 对照组是王道：4/22 成功案例和 4/24 失败案例在日志里表现相同 —— 这一条就足以推翻"通过日志看投递成功与否"的整条推理链

### 详细任务档
`memory/tasks/mangba-delivery-20260424.md`（含前两版推翻的推理过程，供以后 debug 参考，不删）

## 01:47 mangba 微信 cron 投递问题收尾
- Bruce 确认 4/22、4/23 的 20:00 学习与 22:00 晚报均实际收到；4/24 没收到。重新推导后定位为 iLink outbound 冷会话/约 24h token TTL 问题，不是 Bruce 记错。
- 已打补丁 1：`openclaw-weixin/src/api/api.ts` 解析 sendMessage 响应 body；`ret!=0` 抛错，避免 HTTP 200 + `{"ret":-2}` 被标成 delivered=true。
- 实验：直接调用 iLink `getConfig`（带/不带 stale context_token）均 ret=0；随后 no-token sendMessage 与 stale-token sendMessage 两条测试消息 Bruce 都收到。
- 关键验证：getConfig warm-up 后再次触发原 mangba cron，runs 显示 `status=ok delivered=true`，Bruce 微信侧收到测试推送。
- 已打补丁 2：`openclaw-weixin/src/channel.ts` 在 cron/outbound 发送前执行 `getConfig` warm-up；遇到 ret=-2 时 no-context warm-up 后重试一次。
- Gateway 01:46 重启成功，5 plugins loaded，4 个 weixin bot auto-restart 后 monitor started；补丁行已在源文件确认。未再触发额外 cron，避免打扰。
- 遗留：openclaw-weixin 启动时偶发 `Cannot read properties of undefined (reading 'logger')`，但 auto-restart 5s 后恢复；另需后续向上游提交 issue/PR。
→ task: memory/tasks/mangba-delivery-20260424.md
