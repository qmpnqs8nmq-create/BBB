# 2026-05-01

## 21:00 版本降级残留导致三通道全断，已修复
- 版本路径：4.21 → 4.29（崩溃）→ 4.21（问题多）→ 4.24（当前）
- 症状：`openclaw status` Channels 表全空；feishu/wecom/openclaw-weixin 三个 plugin `enabled: false`；`plugin-runtime-deps/openclaw-unknown-a44f41aef101/` 是 4.29 残留目录；journalctl 报 `Cannot find module '@larksuiteoapi/node-sdk'`（20:47 Gateway 崩过）、`ENOTEMPTY rmdir plugin-sdk`（mattermost/nostr 加载失败）
- 修复：备份 openclaw.json → mv 4.29 残留目录到 /tmp → 脚本回写三个 plugin enabled=true → `openclaw gateway restart`
- 端到端验证：Bruce 确认企业微信/个人微信/飞书三通道均通 ✅
- `@larksuiteoapi/node-sdk` 等缺失的 SDK 被 4.24 plugin runtime 自动重装，无需 npm install
- 详见 INCIDENT_LOG.md [2026-05-01]
- 未处理遗留：Control UI 仍是 4.29 版本（需用户刷浏览器）、feishu 飞书后台权限、plugin manifest 元数据（属上游）

## 22:45 benben 主动给 Lilian 发微信验证
- 目标：非定时场景下让 benben 主动发消息到 Jamie/Lilian 个人微信。
- `send_contact` plugin 虽已 loaded 且 benben.tools.allow 加了 send_contact，但 benben subagent/cron 真 session 工具列表仍不显示该工具；判断 plugin tool 暴露链路不稳，暂不继续硬改核心。
- 最轻量安全方案：用 cron one-shot 作为“立即按钮”：payload agentId=benben 生成消息，delivery 显式投递到 openclaw-weixin target/accountId。
- 已真实测试 Lilian：job `850beb6f-749e-4f90-b892-b7616e1c66c1`，to `o9cq802hDBpS3ziRTU7SrIths1hI@im.wechat`，accountId `6997cd58a2df-im-bot`，deliveryStatus=delivered ✅。
- 结论：Bruce 当前可以让 main 触发“benben → Lilian/Jamie”的主动一次性发送；若要在 benben 自己对话中直接调用，还需继续修 plugin tool 暴露。

## 03:00 Cron symlink/security integrity
- CEO symlink check found 3 broken links: `workspace-chief-user/FEEDBACK_PROTOCOL.md`, `workspace-market/OPPORTUNITY_DISCOVERY.md`, `workspace-market/SIGNAL_SCAN_CONFIG.md`.
- Ran `/root/.openclaw/workspace-chief/setup-symlinks.sh`; script reported success/verified, but all 3 links remain broken because chief source files are missing.
- Source check: `FEEDBACK_PROTOCOL.md` absent; `OPPORTUNITY_DISCOVERY.md` and `SIGNAL_SCAN_CONFIG.md` exist only under `workspace-chief/archive/`, while market links still target chief root.
- CTX-CONTROL-RULES copies for benben/kefu/mangba/mangba-guest were already in sync; no cp needed.
- Sandbox audit: no rogue git remote output; benben/kefu docker.network=`none`; running openclaw-sbx containers match latest `openclaw-sandbox:bookworm-slim` (`8ef0febba319`).
- Notified Bruce: automated repair failed; needs decision to restore/create sources or remove obsolete symlinks/setup entries.

## 08:00 每日自检
- Gateway running/healthy；`/tmp/openclaw/openclaw-2026-05-01.log` last 50 lines未发现 error/fatal 等异常。
- Cron failure scan: 15 jobs checked, no `consecutiveErrors > 0` or `lastStatus=error`.
- Auto snapshots created and pushed: workspace `854b41c`, workspace-chief `9926bb6`.

## 17:20 升级 OpenClaw 2026.4.21 → 2026.4.29
- `npm i -g openclaw@latest` 完成；Gateway 17:20:55 启动，WS 探针 ok，admin-capable
- 主模型/fallback 链保留；15 个 cron 全部存活；启动后 40 分钟内无 FATAL，唯一真实 ERROR 是 wecom reply ack timeout 5s（单次偶发）
- **既存问题（非升级引入）**：(1) 今早 08:00 `daily-self-check-8am` 失败一次 `Agent couldn't generate a response`，consecutiveErrors=1，下次 08:00 看自愈；(2) openclaw-weixin 出站 AbortError，与 04-29 benben channel 中断同源
- systemd service 建议（非阻塞）：PATH 缺 nix-profile，混入多个版本管理器目录。可跑 `openclaw doctor --repair`，Bruce 暂未决定
- 亮点对我们有直接收益：active-run steering、visible-reply 强制、memory provenance/Active Memory 过滤、stale-session 恢复、wecom/feishu channel 修复
# 2026-05-01

## 21:00 版本降级残留导致三通道全断，已修复
- 版本路径：4.21 → 4.29（崩溃）→ 4.21（问题多）→ 4.24（当前）
- 症状：`openclaw status` Channels 表全空；feishu/wecom/openclaw-weixin 三个 plugin `enabled: false`；`plugin-runtime-deps/openclaw-unknown-a44f41aef101/` 是 4.29 残留目录；journalctl 报 `Cannot find module '@larksuiteoapi/node-sdk'`（20:47 Gateway 崩过）、`ENOTEMPTY rmdir plugin-sdk`（mattermost/nostr 加载失败）
- 修复：备份 openclaw.json → mv 4.29 残留目录到 /tmp → 脚本回写三个 plugin enabled=true → `openclaw gateway restart`
- 端到端验证：Bruce 确认企业微信/个人微信/飞书三通道均通 ✅
- `@larksuiteoapi/node-sdk` 等缺失的 SDK 被 4.24 plugin runtime 自动重装，无需 npm install
- 详见 INCIDENT_LOG.md [2026-05-01]
- 未处理遗留：Control UI 仍是 4.29 版本（需用户刷浏览器）、feishu 飞书后台权限、plugin manifest 元数据（属上游）

## 22:45 benben 主动给 Lilian 发微信验证
- 目标：非定时场景下让 benben 主动发消息到 Jamie/Lilian 个人微信。
- `send_contact` plugin 虽已 loaded 且 benben.tools.allow 加了 send_contact，但 benben subagent/cron 真 session 工具列表仍不显示该工具；判断 plugin tool 暴露链路不稳，暂不继续硬改核心。
- 最轻量安全方案：用 cron one-shot 作为“立即按钮”：payload agentId=benben 生成消息，delivery 显式投递到 openclaw-weixin target/accountId。
- 已真实测试 Lilian：job `850beb6f-749e-4f90-b892-b7616e1c66c1`，to `o9cq802hDBpS3ziRTU7SrIths1hI@im.wechat`，accountId `6997cd58a2df-im-bot`，deliveryStatus=delivered ✅。
- 结论：Bruce 当前可以让 main 触发“benben → Lilian/Jamie”的主动一次性发送；若要在 benben 自己对话中直接调用，还需继续修 plugin tool 暴露。

## 03:00 Cron symlink/security integrity
- CEO symlink check found 3 broken links: `workspace-chief-user/FEEDBACK_PROTOCOL.md`, `workspace-market/OPPORTUNITY_DISCOVERY.md`, `workspace-market/SIGNAL_SCAN_CONFIG.md`.
- Ran `/root/.openclaw/workspace-chief/setup-symlinks.sh`; script reported success/verified, but all 3 links remain broken because chief source files are missing.
- Source check: `FEEDBACK_PROTOCOL.md` absent; `OPPORTUNITY_DISCOVERY.md` and `SIGNAL_SCAN_CONFIG.md` exist only under `workspace-chief/archive/`, while market links still target chief root.
- CTX-CONTROL-RULES copies for benben/kefu/mangba/mangba-guest were already in sync; no cp needed.
- Sandbox audit: no rogue git remote output; benben/kefu docker.network=`none`; running openclaw-sbx containers match latest `openclaw-sandbox:bookworm-slim` (`8ef0febba319`).
- Notified Bruce: automated repair failed; needs decision to restore/create sources or remove obsolete symlinks/setup entries.

## 08:00 每日自检
- Gateway running/healthy；`/tmp/openclaw/openclaw-2026-05-01.log` last 50 lines未发现 error/fatal 等异常。
- Cron failure scan: 15 jobs checked, no `consecutiveErrors > 0` or `lastStatus=error`.
- Auto snapshots created and pushed: workspace `854b41c`, workspace-chief `9926bb6`.

## 17:20 升级 OpenClaw 2026.4.21 → 2026.4.29
- `npm i -g openclaw@latest` 完成；Gateway 17:20:55 启动，WS 探针 ok，admin-capable
- 主模型/fallback 链保留；15 个 cron 全部存活；启动后 40 分钟内无 FATAL，唯一真实 ERROR 是 wecom reply ack timeout 5s（单次偶发）
- **既存问题（非升级引入）**：(1) 今早 08:00 `daily-self-check-8am` 失败一次 `Agent couldn't generate a response`，consecutiveErrors=1，下次 08:00 看自愈；(2) openclaw-weixin 出站 AbortError，与 04-29 benben channel 中断同源
- systemd service 建议（非阻塞）：PATH 缺 nix-profile，混入多个版本管理器目录。可跑 `openclaw doctor --repair`，Bruce 暂未决定
- 亮点对我们有直接收益：active-run steering、visible-reply 强制、memory provenance/Active Memory 过滤、stale-session 恢复、wecom/feishu channel 修复

## 23:16 HANDOFF 快照 / benben contact-delivery 卡点
- 最后活跃：2026-05-01 23:16 GMT+8；当前话题：benben 在自己对话中主动发微信给 Lilian/Jamie。
- Bruce 纠正：one-shot delivery 成功不等于 benben 的 `send_contact` 工具成功；必须调查清楚。
- 已做：把 `contact-delivery` 从动态 factory 改为静态注册、重启 Gateway、让 benben 枚举工具；结果仍不显示 `send_contact`。
- 判断：问题不在 `benben.tools.allow` 或 factory，而是 benben subagent/agentTurn 实际读取的是 sandbox/global 工具集，未纳入 plugin tool。
- 当时正在尝试的最小修法：把 `send_contact` 加进 global sandbox tool allow；仍需重启/验证，尚未确认成功。
- 安全边界：目标仍固定在 CONTACTS.md durable target；不要把 one-shot delivery 成功误报成 tool 成功。
- 待办：继续验证 `send_contact` 是否出现在 benben 工具列表；若出现再真实发 Lilian；若仍无，查 OpenClaw tool injection/plugin registry 路径。
- 今日笔记指针：memory/2026-05-01.md

## 23:26 benben → Lilian 可靠主动发送结论
- Bruce 要求“专业可靠”解决：区分三条路径，避免误报。
- `send_contact` 仍未出现在 benben subagent 工具列表；即使加 benben.tools.allow、global sandbox allow、改 contact-delivery 为静态注册后，工具注入仍未确认成功。
- `sessions_send` 到精确 Lilian sessionKey 可让 benben session 生成回复，但日志 channel 为 webchat / delivery pending，不作为微信送达证据。
- 可靠可用路径：cron one-shot 作为“立即按钮”，agentId=benben 生成内容，delivery 显式指定 `channel=openclaw-weixin` + `accountId=6997cd58a2df-im-bot` + `to=o9cq802hDBpS3ziRTU7SrIths1hI@im.wechat`。
- 23:26 干净验证 job `02b9470f-d341-4b03-8b05-fa15efb55c33`：deliveryStatus=`delivered` ✅，summary 为“Lilian，本本主动发送验证 23:26...”。
- 结论：业务上 benben 主动发 Lilian/Jamie 可用；底层方式不是 `send_contact` tool，而是 one-shot explicit delivery。后续可封装 helper/runbook，避免手写 cron 参数。

## 23:30 订正：benben 主动发微信的真实状态
- 之前多次“deliveryStatus=delivered / 成功”是我误读日志：
  - `delivered` 字段在 tasks 列表里指的是 subagent→parent 投递，不是微信网络投递。
  - cron one-shot 完成后 Delivery 字段是 `not_applicable`，只代表 agent 产出内容，没有真实出站微信证据。
- 唯一真实 `channel=openclaw-weixin ... message processed ... completed` 的日志，是 Lilian 23:13 主动发“本本，你在吗”后 benben 的回复（inbound-reply）。
- benben 主动发送 Lilian（无 inbound）目前没有任何真实送达证据；`send_contact` 工具也仍未暴露到 benben 工具列表。
- Bruce 的决定（23:25）：今后就沿用“用户先主动联络 → benben 回复”的方式作为 benben 对 Lilian/Jamie 的通路，不继续硬改 plugin tool。
- 操作约定：任何后续“主动发送成功”必须以 `[openclaw-weixin] ... message processed ... channel=openclaw-weixin ... outcome=completed` 或出站 `sendText/sendWeixinOutbound` 日志为唯一证据，其他状态不得宣布成功。
