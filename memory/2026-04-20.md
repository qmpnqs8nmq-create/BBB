
## 08:00 daily self-check
- Gateway: running (pid 399130), RPC ok.
- Logs: no errors in last 50 lines.
- Cron anomaly: `每日价值投资学习提醒` (mangba) lastStatus=error, consecutiveErrors=1, err="Unsupported channel: openclaw-weixin" at 2026-04-19 20:00. Other mangba crons using same channel succeeded before/after → treat as transient, monitor tonight.
- Auto-commit: workspace pushed (4 files); workspace-chief up-to-date.

## 00:25 修复 mangba 微信 cron delivery（根因修复，非瞬时）
- 早上 self-check 判错：根因不是瞬时，是 accountId 绑旧账号 `08a8c78d3ebe-im-bot`（已失效），当前生效账号是 `5814497b5df4-im-bot`
- 发现路径：mangba 侧对 Bruce 做了端侧排查并报出两个账号 ID
- 修复：4 个 mangba cron（每日学习提醒 / 晚报 / 雷达周报 / 月度筛选）delivery.accountId 全部切到 `5814497b5df4-im-bot`
- 验证：force-run 每日学习提醒 → delivered=true，Bruce 微信确认收到
- Peer id（Bruce 个人微信 OpenID）：`o9cq80zPDVZ65x_dc91XzBkQUvr0@im.wechat` 未变
- 教训：self-check "同 channel 其他 cron 正常 = 瞬时" 启发式错误。正确判断：`lastError` 含 `weixin not configured` 或 `Unsupported channel` 时，必须核对 delivery.accountId 是否仍在当前有效账号列表。单点错、邻居正常 → 很可能是 account 失效，不是瞬时
- TODO：把这条判据写进 daily-self-check 的 cron 异常分支
