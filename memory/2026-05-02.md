
## 12:15 mangba-guest 朋友接入流程定型
- 朋友用个人微信接入 benben 旗下 `mangba-guest` agent。
- 试错：`openclaw channels login --channel openclaw-weixin` 会给出 `liteapp.weixin.qq.com/q/` 链接，但**几分钟就失效**，不能提前囤。
- 正确协作模式：Bruce 说"发链接" → 现场起 login PTY + autobind 监听脚本（diff accounts/*.json 新增）+ 通知 cron → 检测到新账号自动写 binding（agentId=mangba-guest）+ 重启 Gateway + 回报。
- 此次第一版链接已过期并清理（process + cron + 临时文件）。等朋友就绪再起。
- 已写入 MEMORY.md Misc。
