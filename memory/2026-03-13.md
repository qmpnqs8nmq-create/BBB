- Bruce introduced himself as new and set my identity: name **Kaopuge**, creature **世界最优秀的CEO**, vibe **偏冷静、带幽默**, emoji **🐎**.
- User prefers to keep using the current **browser-based Dashboard + local Gateway** setup for now, instead of switching to a native OpenClaw.app flow.
- Docker Desktop for Mac (Apple Silicon) was downloaded and installed successfully; Rosetta 2 was installed to fix the initial Docker onboarding error, and Docker was verified working (`docker version` succeeded).
- OpenClaw local setup status: Gateway is running locally and Dashboard is reachable; user was advised to choose **This Mac / Local** for macOS onboarding guidance, but in practice they are operating through the browser dashboard rather than a native macOS app.
- Workspace setup: `~/.openclaw/workspace` is a git repo, `.gitignore` was added to ignore `.openclaw/`, and commits were made to capture identity/user/workspace defaults.
- GitHub backup progress: user created private repo `https://github.com/qmpnqs8nmq-create/BBB.git`; local git remote `origin` now points there, but push is still pending GitHub authentication via PAT.
