# 日报 2026-05-04

## 🏢 业务进展（chief）
- 今天核心是围绕 ADR-012 完成 chief/main 的回滚后同步与治理收口，最终确认当前 CEO 系统主线为 **Route 1/2/3 + 5-agent**；Dual-Engine / Radar Operating 已被 ADR-012 superseded / archived，仅作为历史来源保留。
- Bruce 要求双方再次确认问题是否同步、处理完。chief 侧先后修正了 `NORTH_STAR.md`、`CEO_SYSTEM_NAVIGATION.md` 中的 pointer drift：ADR-004/005/006/008/010/011 不再被当作当前运行主干，Dual-Engine/Radar 只保留历史引用。
- chief 侧继续收口 `memory/kill-criteria.md`：从原先仍引用双引擎/雷达的 v2，升级到 v2.1 / v2.2，保留 `$10M` 运作资金池、`$3M` 亏损底线、单笔 `$300K` 硬线，同时把 Product-level Kill 抽象成适用于 Route 1/2/3 与未来候选项目的通用治理框架。
- `memory/dual-engine-playbook.md` 与 `memory/dual-engine-playbook-v2-2026-04-25.md` 已加 ADR-012 archived header，移除“v2 当前有效版本”的错误口径。
- 治理规则层面，chief 在 `AGENTS.md` Pre-change Gate 与 `GOVERNANCE.md` 增加说明，引用 main workspace 的 `CTX-CONTROL-RULES.md §8` 作为 single source of truth，不复制条文，避免多 workspace 规则分叉。
- memory_search 侧已实测 `openai/text-embedding-3-small + hybrid + MMR` 可用，并更新了 chief `AGENTS.md` 与 `MEMORY.md` 中旧 fallback 叙述。
- chief 当前结论：ADR-012 对齐问题全部收口，无剩余 chief action needed。

## 🔧 系统运维（main）
- 08:00 自检：Gateway running、probe OK；发现 03:00 一次 cron delivery transient error，以及 `信号雷达周报` 一次 weixin channel 配置相关错误，其他 mangba weixin crons 正常。
- OpenClaw 从 `2026.4.24 (cbcfdf6)` 升级到 `2026.5.2 (8b2a6e5)`；升级后基线审计显示 Gateway 正常，但服务器存在公网监听与防火墙缺失风险，OpenClaw audit 有 1 critical + 5 warnings，OS 有 44 个待更新包。今天未做大范围修复。
- 升级后复查：systemd Gateway enabled/running，connectivity probe OK/admin-capable；journal 无 failed units，资源健康，无升级导致的重大故障迹象。
- Bruce 仅批准修复 service PATH warning。main 备份 user service unit 后更新 Description、`OPENCLAW_SERVICE_VERSION=2026.5.2`，收窄 PATH，并 reload/restart Gateway；最终 Gateway 正常。
- 由于 Bruce 指出 chief 不应掌握 main 内部台账/全局审计细节，main 收紧了对 chief 的信息暴露：只给结论与对 chief 的要求，不暴露全局 cron diff、SYSTEM_CHANGE_LEDGER、symlink 机制、非 CEO workspace 审计等内部细节。
- 建立 `memory/SYSTEM_CHANGE_LEDGER.md` 作为全局系统变更台账，并明确平台/系统回滚、升级、全局 cron 清理、workspace-wide restore 由 main 负责；chief 只能请求/提议，不能独立执行。
- main 更新并传播 `CTX-CONTROL-RULES.md §8` 到多个 workspace，确立 system-level change DRI = main。

## ⏳ 遗留 / 待办
- 系统安全与运维 hardening 仍未处理：公网监听、防火墙缺失、OpenClaw audit 1 critical + 5 warnings、OS 44 个待更新包需要后续排期。
- `信号雷达周报` cron 出现一次 weixin channel 配置相关错误，当前仅记录为单次错误；若连续出现需检查 channel login / account 绑定。
- sessions visibility / A2A direct communication 今天已验证可用，但此前曾因限制改走文件桥；后续如出现 session lock/probe timeout，需要专门诊断。
- 今日 ADR-012 / rollback cleanup 已无已知 blocker。

## 💡 洞察
- 今天最大改进不是单个文件修正，而是把“谁有权做系统级回滚/恢复”这个责任边界明确下来：main 是平台 DRI，chief 负责业务治理，不再让 Bruce 在两个 workspace 之间人工协调。
- chief 业务叙事已从 Dual-Engine/Radar 成功迁移到 ADR-012 后的 Route 1/2/3 + 5-agent 主线；剩余历史材料通过 archived / provenance 保留，既不丢上下文，也不误导当前运行。
- main 的信息披露边界也被校准：跨 agent 协作应该输出结论和行动要求，而不是把平台内部审计细节扩散给业务 agent。