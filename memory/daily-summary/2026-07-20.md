# 日报 2026-07-20

## 🏢 业务进展（chief）
- 今日无 chief 日志（`workspace-chief/memory/2026-07-20.md` 不存在）。
- chief HANDOFF 仍停留在 2026-05-04（ADR-012 回滚同步收尾），今日无新业务决策或经营动作记录。
- 待跟进：chief 侧长期无活动，可确认业务线是否需要重新激活或归档。

## 🔧 系统运维（main）
今日为高强度运维日，主线是 benben 检索系统的完整排障与修复：

**benben memory_search 三层问题全部定位并修复**
1. **索引 identity mismatched**（旧 providerKey 残留，7/13 beta.6 升级后遗留）：备份 562MB SQLite 后 `--force` 重建，检索恢复正常（最高分 0.781）。
2. **重建后仍超时**：根因为 hybrid MMR 同步重排阻塞事件循环（maxResults=20 需 46.6s）。经 Bruce 确认，仅对 benben 关闭 MMR，三次 20 条检索均 ~6.3s 通过验收。备份+热加载，未重启 Gateway。
3. **Codex 子代理动态工具桥接缺口**：官方 handler 仅匹配父线程 threadId+turnId，beta 升级无收益。落地规避方案：在 benben 的 TOOLS.md/AGENTS.md 固化"父线程集中检索→证据包分流给子代理"规则，隔离会话验证注入完整。另发现 memory-wiki 2946 页 digest 回退全量读取拖慢 wiki 检索（18s），已记录 task 文件。

**其他**
- 全 agent embedding 只读审计（11 个，排除 benben）：全部 valid、真实检索通过；仅 kefu 索引 mismatched，Bruce 判定可能作废不修。
- GPT-5.6 Sol：确认 "250k cap" 是 7/13 本地自定义配置而非 OAuth 限制（实际窗口 272k）；显示名恢复为 `GPT-5.6 Sol`。
- 晨间自检：ZenMux 两 key quota 402 + OpenAI 账户 not active，导致 CEO Weekly Briefing 连续失败 3 次；4 个 cron 曾报错。
- 日常维护：日快照提交推送、2026-07-06 日志归档、MEMORY.md 压缩 91→71 行。

## ⏳ 遗留 / 待办
- **benben 端到端验收**：需 Bruce 在 benben 发送 `/new` 后做真实全面检索验收。
- **模型额度**：ZenMux 两 key quota 超限、OpenAI 账户不可用，需 Bruce 处理 provider 额度/账户（影响 CEO Weekly Briefing 等 cron）。
- memory-wiki digest 0 claims 导致 wiki 检索慢，拟修复方案在 `memory/tasks/benben-memory-search-root-cause.md`。
- kefu 索引 mismatched（已决定不修，待确认是否归档该 agent）。
- chief 侧长期无日志，确认业务线状态。

## 💡 洞察
- 今日排障范式值得沉淀：**分层单测（provider→网络→DB→算法）+ A/B 配置对比**快速把"超时"从表象追到 MMR 算法层，避免了误判为 provider 故障。
- 版本升级（beta.6）造成的 providerKey 指纹变化是慢性雷：main/chief 早修了，benben 拖到 7/18 才暴露。建议升级后主动对全 agent 跑一轮 index identity 检查。
- 每次变更前先备份 + 热加载优先于重启的纪律执行良好，全天 0 次 Gateway 重启。
- 模型额度问题反复影响多个 cron，是当前最需要 Bruce 人工介入的事项。
