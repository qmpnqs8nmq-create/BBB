# 日报 2026-06-12

## 🏢 业务进展（chief）
- 今日未发现 chief 日志文件：`/root/.openclaw/workspace-chief/memory/2026-06-12.md` 不存在，因此没有可确认的业务/经营/决策类新增记录。
- chief 状态快照 `HANDOFF.md` 的最后活跃时间为 2026-05-04，内容是 ADR-012 rollback sync 已收尾、chief 无需行动；该快照不是今日记录，仅作为“无今日 chief 增量”的佐证。
- 严格按文件记录，本日没有可归纳的 chief 业务决策、经营动作或项目进展。

## 🔧 系统运维（main）
- Bruce 主导排查 OpenClaw fallback / 子 agent failover 问题，确认 402 满额不自动切换的关键链路：ZenMux 返回 `"code":"402"` 字符串码值，但 `RAW_402_MARKER_RE` 只识别未加引号的 `402`，导致下游 rate_limit 分类和 fallback 决策都没有触发。
- 同时确认 failover 决策点拿到的是被收敛后的 `"LLM request failed."`，丢失 status 与原始错误文本；`agents.defaults.subagents.model` 配置存在“报告值与执行值不一致”现象，子 agent 实际仍从 primary key1 起步，撞欠费 402 后 0 token 秒死。
- 已修正 `billingBackoffHoursByProvider` 的真实 provider key：从不存在的 `custom-zenmux-ai` 改为 `zenmux-key1` / `zenmux-key2`，窗口设为 1h。
- 按 Bruce “不管什么办法，跑通”的要求，对 OpenClaw 编译产物 `dist/errors-DcOiGp7S.js` 做外科补丁：将正则从 `[:=]\s*402\b` 改为 `[:=]\s*["']?402\b`，允许带引号的 402 被识别。原文件已备份到 `/root/.openclaw/backups/errors-DcOiGp7S.js.orig-*`。
- 端到端验证通过：子 agent 从欠费 key1 起步，命中 402 后出现 `reason=rate_limit` 与 `failover decision=fallback_model`，自动切到 fallback 并完成任务。后续真实欠费场景复测也通过，17×23 返回 391。
- 后续又将补丁改为自愈方案：新增幂等脚本 `workspace/scripts/patch-402-failover.py`，并通过 systemd user drop-in `~/.config/systemd/user/openclaw-gateway.service.d/10-patch-402.conf` 在 Gateway 启动前自动重打补丁，已重启验证 `status=0` 且补丁在位。

## ⏳ 遗留 / 待办
- node_modules 编译产物补丁仍是本地外科修复，OpenClaw 升级会覆盖；当前通过 ExecStartPre 自愈脚本缓解。
- 上游 issue 草稿曾记录在 `memory/tasks/openclaw-402-subagent-failover-issue.md`，但 Bruce 已决定不提交上游 issue；该 task 文件可保留作 bug 技术档。
- 长期根治仍依赖 OpenClaw 上游修复：402 字符串码值识别、错误收敛丢 status、以及 subagents.model 执行值不一致。

## 💡 洞察
- 今天的核心价值集中在 main 系统层：从“fallback 理论上配置了但实际不工作”的体验问题，追到可复现实证、代码根因、本地补丁、端到端验证，再到启动自愈，形成了可持续运行的闭环。
- chief 今日没有可用新增记录，说明本次日报的业务部分只能如实留空；后续若希望业务日报占比稳定，需要确保 chief 工作区当日日志持续落盘。
