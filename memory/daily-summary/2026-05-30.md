# 日报 2026-05-30

## 🏢 业务进展（chief）
- 今日无 chief 业务/经营/决策记录（`workspace-chief/memory/2026-05-30.md` 不存在）。
- chief 状态快照仍停留在 2026-05-04（ADR-012 rollback sync 已闭环，无待处理 chief action），近期无新增业务动作。

## 🔧 系统运维（main）
- **memory_search embedding provider 迁移**：因 OpenAI 额度 429 耗尽，将 `agents.defaults.memorySearch` 从 OpenAI 切换到 Gemini。
  - 配置：provider=`gemini`（非 `google`），model=`gemini-embedding-001`（3072 维），auth 走 `google:default`(api_key) 自动映射。
  - 操作要点：改 memorySearch 后必须 restart Gateway（进程缓存旧配置），且换 provider 后必须 `openclaw memory index --force` 全量重建（维度 1536→3072 不兼容）。
  - 验证：memory_search 实测正常（provider=gemini，vectorScore 0.77+），429 消失；status 显示 Vector dims=3072、Embeddings ready。
  - 已备份：`backups/openclaw.json.bak.embedding-google-*`。

## ⏳ 遗留 / 待办
- OpenAI api_key embedding 额度待恢复 / 评估是否长期保留 Gemini 为主。
- 注意 OpenAI OAuth(openai-codex) 无 `/v1/embeddings` 端点，不能用于 embedding，仅 api_key 类型可用（已记录避坑）。

## 💡 洞察
- 今日数据偏 main（运维）单边，chief 侧无落盘记录——若实际有业务活动，提示 chief 日志增量写入纪律需加强。
- provider 命名陷阱（`gemini` vs `google`）已沉淀到 HANDOFF，后续切换 embedding 可直接复用。
