# 日报 2026-05-29

## 🏢 业务进展（chief）
- 今日无 chief 业务记录（`workspace-chief/memory/2026-05-29.md` 不存在，chief HANDOFF 仍停在 2026-05-04 的 ADR-012 收尾）。
- 即：今天没有新的经营决策、项目推进或业务待办产生。

## 🔧 系统运维（main）
- **08:00 日常自检**：Gateway 正常运行（pid 1689797），无最近 error 日志；workspace + chief 已自动 snapshot push。发现 3 个历史失败 cron（周日系统巡检 5b1abbd9/7a59f223、NVDA Q1 FY27 抓取 ×2，均为旧失败非今日新增），决定下次运行时观察恢复，暂不自修复。
- **10:30 ZenMux 模型升级**：在 Bruce 主导下验证 ZenMux 确有 opus-4.8，将 opus 全线从 4.7 升到 4.8 并彻底删除 key3。关键发现——模型配置有 **3 层合并来源**（openclaw.json + 顶层 models.json/auth-profiles.json + 24 个 agent 各自的同名文件），不全改 key3 会"复活"。用脚本批量处理，全量备份在 `backups/zenmux-key3-cleanup-20260529-105511/`。验证 models list 仅剩 key1/key2 ×(opus-4.8 + sonnet-4.6)。
- **15:45 OpenClaw 升级 5.18 → 5.27**：理由为安全加固（群 prompt 不进 system、记忆按不可信处理、SSRF/路径穿越修复、无 auth Tailscale 拒绝）+ 性能/召回改进 + cron 并发默认提到 8。升级前备份 27 个配置文件，config validate 通过，restart 后 CLI/Gateway 版本一致且 running，三层配置无旧 key 复活、无幽灵 sonnet-4.8，fallback 链完整，35 个 cron enabled。

## ⏳ 遗留 / 待办
- 3 个 error 状态 cron 待下次运行观察是否自愈：周日系统巡检（7a59f223）、NVDA Q1 FY27 抓取（890f4b0a / 5b1abbd9，HTML 错误页 / CDN 拦截）。
- 升级后副作用确认：6 个无 tag 的 codex/* 本地 harness 目录消失（codex CLI 不在 PATH，残留探测目录，判定无害，无需处理）。

## 💡 洞察
- 今日全部活动集中在系统/运维层，业务侧（chief）静默。日报"2/3 业务 + 1/3 运维"的比例无法满足，源于今天确实没有业务输入——属正常，不强行凑内容。
- 模型配置的"3 层合并"是高频踩坑点，已沉淀进 HANDOFF；改模型务必三层同步，否则删除项会复活。
- restart 会以 SIGTERM 断开当前 webchat 连接属正常现象，等几秒重连即可——后续做配置变更可提前告知 Bruce 避免误判。
