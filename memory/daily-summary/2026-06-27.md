# 日报 2026-06-27

## 🏢 业务进展（chief）
- 今日未找到 `/root/.openclaw/workspace-chief/memory/2026-06-27.md`，因此没有可用于提炼的当日 chief 业务/经营/决策记录。
- chief 的 HANDOFF 仍停留在 2026-05-04，内容是 ADR-012 rollback sync 已关闭、Route 1/2/3 + 5-agent 为主线、chief 当前无行动项；该状态快照较旧，只作为背景，不作为今日进展。

## 🔧 系统运维（main）
- 08:30 复核 Perplexity 配置：当前 `tools.web.search.provider=perplexity`、`timeoutSeconds=60`、插件层 API key 仍兼容；6/26 已移除会触发 legacy path 的 `webSearch.model=sonar-pro`，暂不需要回滚。后续若追求标准化，可迁移 key 到 `tools.web.search.perplexity.apiKey`。
- 08:50 定位 benben 的 `Dummy.list Mcp Resources` 报错：并非 MCP 配置缺失，而是模型/工具路由误把占位名 `dummy` 当成 MCP server 调用；静态配置未发现 dummy server。
- 21:55 定位 benben `The agent run failed before producing a reply`：根因是会话历史重放状态损坏，日志为 `messages.5.content.0: Invalid signature in thinking block` 和 `Session history or replay state is invalid. Use /new...`；不是上下文超限，也不是 dummy MCP。

## ⏳ 遗留 / 待办
- Perplexity 配置可择机标准化：把 API key 从插件层迁移到 `tools.web.search.perplexity.apiKey`，继续不设置 `model/baseUrl`。
- 若 dummy MCP 误调频繁复发，再考虑清理相关 Codex session/cache 或增加提示约束。
- benben 当前损坏会话的最快恢复方式是在窗口中 `/new` 或开新会话；若要彻底清理/归档/重置该 session 文件，需要 Bruce 确认。

## 💡 洞察
- 今日主要工作集中在 main 侧的系统诊断与稳定性排查，尤其是区分「配置问题」「工具路由误调」「会话历史损坏」三类不同故障，避免把 benben 的多个报错混为同一个根因。
- chief 侧今日无新增业务日志，日报中业务部分只能保守记录为缺失；后续若希望日报更有经营视角，需要 chief workspace 当日持续落盘。
