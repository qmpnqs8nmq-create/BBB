## 08:04 每日自检
- Gateway 正常，最近 50 行日志无 ERROR；两仓库已完成每日快照并推送尝试。
- `symlink-integrity-check` 曾失败，手动重跑后恢复（consecutiveErrors=0）。
- `Validation Tracker (Midweek Nudge)` 连续失败 5 次（ZenMux 配额耗尽、OpenAI 账户未激活）；已改用已验证可用的 Gemini 模型并手动重跑，状态恢复 ok、consecutiveErrors=0。

## 08:11 每日自检重试
- Gateway 运行与连接探测正常；最近 50 行无 ERROR，仅配置/子代理监控告警。
- `daily-self-check-8am` 前两次因错误日志参数 `--tail 50` 失败；正确命令为 `openclaw logs --limit 50 --plain`。
- 受 cron 自修改权限限制，未能把正确命令固化到任务 payload，需主会话后续修改。

## 08:20 每日自检修复
- Gateway 正常；最近 50 行无 ERROR，仅插件配置与子代理监控告警；两仓库快照命令完成。
- `daily-self-check-8am` 累计 3 次失败，根因是错误日志命令超时；已通过 CLI 将任务固化为 `openclaw logs --limit 50 --plain --no-color`，下次运行验证清零。
