## 10:18 每日自检

- Gateway 运行正常、连通探测通过；最近 50 行日志出现 ZenMux `quote_exceeded`，且 3 模型 fallback 全部失败。
- 4 个 cron 处于 error（连续错误 1–2 次）：每日使用总结、Memory Dreaming Promotion、刘董事长每日价值投资课程、每日价值投资学习提醒；外部额度问题无法安全本地修复。
- 已完成 workspace / workspace-chief 日快照提交（`0d9873c` / `1774ff0`）并尝试推送。

## 10:20 Heartbeat 维护

- 已将 `2026-07-06.md` 蒸馏至 `archive.md` 并移入回收站；MEMORY.md 由 91 行压缩至 71 行，保留了每 agent 重建 memory index 与 ZenMux `/api/v1` 两条长期经验。
- 实时复核仍有模型链故障：两个 ZenMux key 均 quota 402，OpenAI GPT-5.6 Sol 报 account not active，导致 CEO Weekly Briefing 连续失败 2 次；需 Bruce 处理 provider 额度/账户。

## 10:23 每日自检复核

- Gateway 健康；最近 50 行无 ERROR，仅插件配置警告（微信插件重复、WhatsApp/WeCom 配置引用未安装插件）。
- 3 个 cron 标记失败：本自检上轮为偶发 `tail` 超时（现已验证文件正常）；CEO Weekly Briefing 因 ZenMux 额度与 OpenAI 账户不可用连续失败 2 次；价值投资提醒昨晚被 Gateway 重启中断。
- 未自动改 provider/插件配置或补发外部消息，避免破坏现有通道及造成重复投递。

## 10:26 每日自检

- Gateway 运行及连通探测正常；最近 50 行无运行错误，仍有微信插件重复及 WhatsApp/WeCom 缺失警告。
- 失败 cron 共 3 个：daily-self-check（历史超时，当前检查已成功）、CEO Weekly Briefing（连续 3 次，模型额度/账户故障）、每日价值投资学习提醒（Gateway 重启中断）。
- 两个 workspace 已完成日快照并推送尝试；外部模型额度和插件配置未擅自更改。

## 14:00 本本 memory_search 重复报错诊断

- 本本自 7/18 23:09 起反复报 `index provider settings changed`；7/20 13:38 同一轮连续 6 次失败，界面显示的 Tool error 与 trajectory 原始结果一致。
- 深度探针确认 Gemini `gemini-embedding-001` 可用、3072 维向量扩展/FTS 均正常；唯一失败项为 benben 索引 identity mismatched，非 Gateway、模型或 provider 故障。
- benben SQLite 中旧 providerKey `bd508113…` 仍有 5612 处；当前运行时凭据/配置指纹已变化。该类问题在 7/15 chief 已发生并通过逐 agent `--force` 重建修复。
- 触发链：7/13 OpenClaw beta.6 升级后 provider/凭据解析指纹变化；main/chief 已重建或修复，benben 未重建，直到 7/18 再调用才暴露。
- 当前只完成诊断；未改全局配置、未重启 Gateway、未执行 benben 全量重建。修复应仅针对 benben：先备份 SQLite，再 `openclaw memory index --agent benben --force`，完成后做真实检索验证。

## 14:08 本本 memory_search 索引重建完成

- Bruce 确认后，先备份 benben SQLite 至 `/root/.openclaw/backups/benben-memory-reindex-20260720-1405/`（562MB，SHA256 已生成），再执行 `openclaw memory index --agent benben --force`。
- 命令正常完成：`Memory index updated (benben)`；索引 identity 从 mismatched 变为 valid，文件 276→301，chunks 4621→5089，Gemini/向量深度探针正常。
- 真实检索 `Jamie Peddie GPA SAT 1550 基本情况` 返回 5 条结果，最高分 0.781，命中 MEMORY.md 与本本 session；原 `index provider settings changed` 已消失。
- `dirty=true` 仍存在，但 scan=301/301、issues=[] 且实时检索成功，判断为活动 session 持续写入产生的瞬时脏标记，不是索引身份故障；未重启 Gateway，运行态健康。

## 14:35 本本 memory_search 重建后仍超时的完整根因

- Bruce 反馈重建后本本仍报 `memory_search timed out after 15s`；trajectory 确认 14:17:11 发起、14:17:48 返回，属于新故障，不能与旧 providerKey mismatch 混同。
- 分层单测：Gemini 同模型/同长查询直连 0.45–0.67s；OpenClaw SSRF HTTP 包装约 0.5s；sqlite-vec KNN(k=640)约 0.95s；凭据哈希 root/benben 均为 `47eefbaaae16b760`。排除 provider、网络、密钥与数据库损坏。
- 阶梯基准：maxResults=5 → 6.6s，10 → 13.5s，20 → 46.6s。当前 memory tool 源码硬截止 15s、失败后同 agent 冷却 60s；错误包装会误报 embedding/provider。
- 根因：hybrid `candidateMultiplier=4`，向量/关键词候选并集再进入同步 MMR；MMR 实现完整重排全部候选，三重循环式增长并阻塞 Node 事件循环。maxResults=20 时候选最多约160条，15s timer 也被同步计算延迟，故37–47s后才返回“15s超时”。
- 临时配置 A/B：保持 maxResults=20，仅关闭 MMR=6.2s；保持 MMR、candidateMultiplier 4→1=6.5s。生产配置尚未改；建议仅 benben 关闭 MMR，保留候选召回与相关性排序。

## 14:39 会话交接状态

- 当前停在变更授权前：生产配置未修改，等待 Bruce 确认“仅对 benben 关闭 MMR”。
- 获确认后的执行顺序：备份配置 → benben-only 关闭 MMR → 不重启 Gateway（优先热加载）→ 用 benben 运行态、`maxResults=20` 连续真实检索 3 次，记录耗时与结果。
- 验收目标：3 次均在 15 秒内成功，无 `disabled:true`、无 60 秒冷却、无误导性 embedding/provider 报错；失败则回滚并继续定位。

## 14:49 benben MMR 性能修复完成

- Bruce 确认后，备份 `/root/.openclaw/openclaw.json` 至 `/root/.openclaw/backups/benben-mmr-disable-20260720-1443/`，只为 benben 增加 `memorySearch.query.hybrid.mmr.enabled=false`；默认 MMR 与其他 agent 不变。
- 配置 schema 校验通过，Gateway 热加载且 probe OK；未重启、未改 provider、candidateMultiplier、工具超时或索引。
- 三次 `maxResults=20` 独立检索均在外层15秒截止内成功：6.383s / 6.370s / 6.076s，均返回20条。
- Gateway 下 benben 真实 `memory_search(maxResults=20)` trajectory：tool.call 06:47:36.271Z、tool.result 06:47:38.112Z，success=true、20条；无 disabled/timeout/cooldown。
- 首次 synthetic GPT-5.6 Sol 验证仅文本误答“tool unavailable”，trajectory 无 tool.call，不是搜索故障；改用 GPT-5.5 后实际工具调用验收通过。

## 15:20 GPT-5.6 Sol `OAuth 250k cap` 标记复核

- 该标记不是 OpenAI OAuth 账户下发，而是本机 7/13 经 Bruce 确认写入 `openclaw.json` 的自定义模型名；同时设置 `contextWindow/contextTokens=250000`，所以本机确有主动上下文上限。
- 当前 Codex 实时缓存（2026-07-20 14:57 CST）仍返回 Sol `context_window=max_context_window=272000`、effective=95%；250k 位于约 258.4k 有效预算内，属于保守安全余量。
- 其他 OpenClaw 即使复用同一 OAuth，也不会同步这条本地模型覆盖，因此没有括号标签；OAuth 认证与每台机器的模型元数据配置是两层。
- 本轮只读诊断，未改模型配置、认证或 Gateway。

## 15:07 benben 分主题检索 handler 报错复核

- 结论不是索引复坏或 memory handler 随机不稳：benben CLI 当前实时检索成功；原父线程四次分主题查询的 Gateway trajectory 均 `success=true`。
- 父线程的“4 组空结果”是解析错误：`functions.exec` 内动态工具返回 JSON 字符串，代码却读 `r.results`，因此正常结果被压成 `[]`。
- 两个 Codex 原生子代理的 handler 报错是真实的桥接缺口：活动 Codex `2026.7.1-1` handler 仅匹配父线程精确 `threadId + turnId`；子线程继承了工具目录但请求无人处理，落入 app-server 默认错误。
- 当前规避：动态检索由父线程执行并把结果交给子代理；返回值先 JSON.parse 且错误 fail closed。未改配置、索引或 Gateway；已写 INCIDENT_LOG。

## 15:18 子代理动态工具缺口方案比较

- 只读检查 npm 最新 beta `@openclaw/codex 2026.7.2-beta.3`，仍只接受当前父线程精确 threadId + turnId，尚未修复原生子代理动态工具桥接；升级 beta 无收益。
- 不建议本地删除线程校验：该判断同时承担会话归属、权限和审计边界，粗暴放开风险高且升级即失效。
- 当前全局最优是把规避固化为 benben 工作流：父线程集中检索并解析/校验结果，再把证据包交给子代理并行分析；长期向上游报告并等待官方修复。

## 15:25 benben 父线程检索规则落地

- 经 Bruce 确认，在 `/root/.openclaw/workspace-benben/AGENTS.md` 固化父线程检索、JSON 解析校验、错误 fail closed、证据包分流及子线程报错不误重建索引等规则。
- 仅影响 benben 的 Codex 原生子代理动态工具调用方式；原生文件/命令/协作工具不受限。未改 Gateway、模型、provider 或索引，无需重启。
- 备份：`/root/.openclaw/backups/benben-parent-retrieval-rule-20260720-1525/AGENTS.md`。

## 15:30 GPT-5.6 Sol 显示名恢复

- 按 Bruce 要求，将本机 `gpt-5.6-sol` 显示名恢复为 `GPT-5.6 Sol`；250k contextWindow/contextTokens 与128k maxTokens 配置未改。
- 配置校验通过；Gateway PID 前后均为1719、probe OK，未执行 restart。
- 备份：`/root/.openclaw/backups/gpt56-sol-display-name-20260720-1525/openclaw.json`。
- `models list` 当前展示原生目录窗口1050k，配置文件仍保留250k运行覆盖；这是新版目录展示/合并行为，不是本轮扩大配置。

## 16:05 benben 全面检索真实根因

- 父线程运行记录没有加载刚写入的 `workspace-benben/AGENTS.md`；该规则只进入了 Codex 原生子线程，所以此前“已固化父线程规则”的验收结论错误。
- 父线程 6 次 `corpus=all` Promise.all 实际被动态工具层串行化；前三次均约13秒成功，约53秒终止后后三次未发起。整组只在全部完成后输出，故被误判为卡死。
- 每次基础 memory search 仅约1秒；约12秒浪费在 memory-wiki：digest 2946页且0 claims，长查询无完整子串候选时回退读取约2960个Markdown。wiki-only 实测18.11秒。
- 子代理 handler 错误仍是独立桥接缺口，不代表索引损坏。完整证据与拟修复见 `memory/tasks/benben-memory-search-root-cause.md`；本轮未改生产配置或 Gateway。

## 16:18 benben 检索编排本地修复完成

- 在父线程确定加载的 `workspace-benben/TOOLS.md` 写入动态检索约束：先检索后 spawn、子代理禁用 OpenClaw 动态工具、批量默认 corpus=memory、禁用 Promise.all、逐次解析与 fail closed。
- `AGENTS.md` 保留子线程防线，并补充“证据不足则报告、不自行补检索”及 corpus/并发限制；未改索引、embedding、模型配置或 Gateway。
- 隔离新会话 `8d8b3437-3ea6-4d84-9d59-1d719f63fa72` 验证：systemPromptReport 显示 TOOLS 1867 chars、AGENTS 15252 chars 均完整注入；本本准确复述第3/4条，无工具调用。
- 回滚备份：`/root/.openclaw/backups/benben-memory-orchestration-20260720-1612/`。现有旧 benben 会话仍可能保留旧上下文，Bruce 需 `/new` 后再做真实全面检索验收。

## 16:42 全 agent embedding 只读审计（排除 benben）

- 对 main、chief、chief-user、api-worker、strategy、market、execution、coach、mangba、mangba-guest、kefu 分别执行 deep status；均为 Gemini `gemini-embedding-001`、3072 维，embedding probe 正常。
- 前 10 个 agent 的 index identity 均 valid、scan issues=[]；再逐个执行真实 memory search，全部成功返回 5 条，约 5.8–7.7 秒，无 embedding、超时或 provider 错误。
- kefu 唯一异常为独立索引 identity mismatched（`index provider settings changed`），不是 Gemini 服务故障；Bruce 判断该 agent 可能作废，明确无需修复。
- 本轮只读检查，未重建索引、未改配置、未重启 Gateway；benben 按 Bruce 要求未检查。
