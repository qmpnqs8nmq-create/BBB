# 2026-03-16

- Bruce 明确要求将 OpenClaw 浏览器入口按三套隔离：本地管理入口、本地局域网员工入口、Tailscale/远程入口；不同进入方式必须使用不同 token。
- 团队/员工浏览器默认入口必须是 `chief-user`，不能混成 `chief`。本地管理入口仍保留给 `chief`。
- 2026-03-16 晚间为局域网员工入口落地了单独的 `team` profile：默认 agent 为 `chief-user`，独立 token 为 `2c27b0cff0e02af7cfc53223658ded7e3139914ee796cebb`。
- 局域网入口最终采用 HTTPS，而不是 HTTP：当前对外测试地址为 `https://192.168.0.213:8888`。原因是 Control UI 在局域网 IP 的 HTTP 场景下会因 secure context / device identity 失败。
- 局域网入口为自签名 HTTPS 证书方案；员工/设备首次访问时可能需要手动信任证书。
- 局域网 team Gateway 当前后端结构是：HTTPS 入口 `:8888` 反代到 loopback 的 team Gateway `127.0.0.1:8899`。
- 远程/Tailscale 入口保留独立 profile（remote），并生成了独立 token `effb571d2713eeb1adc5beb59e6a0e3cd034f901a91041aa`。
- 2026-03-16 晚间已批准一台 iPhone 局域网设备配对到 team 入口：remote IP `192.168.0.211`，client `openclaw-control-ui`，requestId `5d47eef2-ab91-4586-bac4-1b5065c63f90`。
- 这次排障里一个重要经验：仅验证页面 HTTP 200 不够，必须同时验证浏览器实际 WebSocket 握手、allowedOrigins、secure context、token、pairing 这些链路条件，否则容易误判“已经修好”。
