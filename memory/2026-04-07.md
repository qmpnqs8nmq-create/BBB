# 2026-04-07

## 07:32 系统升级后全面审计

- Bruce 升级到 v2026.4.5 后要求全面审计
- 已完成：UFW 防火墙启用（deny incoming, 放行 22/80/443）、SSH PermitRootLogin→prohibit-password、sysctl symlink 修复
- Gateway bind=localhost 改了但重启后被 Gateway 自动重置回 lan——UFW 兜底已够用，不再折腾
- 删除了 controlUi.allowedOrigins 中的 http://43.106.3.174:18789（仅保留 https）
- market agent profile: full → minimal + 显式 alsoAllow（收紧插件可达性）
- openclaw-weixin 固定版本 @2.0.1
- 08:22 发现 4 个 agent worker 进程吃满 CPU/RAM（每个 500-600MB），重启后恢复正常

## 11:26 重启后二次审计

- 系统资源正常（load 0.12, 2.4GB free）
- 所有渠道正常
- 确认 https://43.106.3.174 可正常访问

## 13:17 Dashboard 模型数量变少

- models list 仅显示 5 个 active 模型（3x opus-4.6 + 2x gpt-5.4）
- sonnet-4.6 已配置但未出现在 Dashboard 选择框（可能是 v4.5 UI 去重/过滤逻辑变更）
- openai-codex:default OAuth token 过期（refresh_token_reused），需要重新登录
- headless 服务器无法交互式执行 auth login，建议 Bruce SSH 或 Dashboard 操作

## 14:20 openclaw-weixin 插件崩溃修复

- v2.0.1 与 v2026.4.5 不兼容，加载报 Maximum call stack size exceeded
- 从 npm pack 手动升级到 2.1.6（ClawHub 被 429 限速）
- 重启后加载正常，所有插件 OK

## 14:42 Dreaming 调度 + 并发配置

- Memory Dreaming 手动开启，调度改为凌晨 5:00
- maxConcurrent 讨论：试改 2 后 Bruce 要求恢复默认 4，继续保持默认
