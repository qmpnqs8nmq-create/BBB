# 2026-03-27

## 08:00 Daily Self-Check
- Gateway: healthy (pid 13875)
- Cron: `market:weekly-intel-scan` has 1 consecutive error — Feishu WSClient delivery failure ("WSClient not connected for account admin"). Transient; next run Monday.
- Logs: yesterday web_search fetch failures ~01:02-01:04 (transient network), harmless tool profile warnings
- Git: both workspace + chief snapshot committed & pushed
- No action required

## 10:22-12:02 152 SSH 隧道修复

### 问题
- 152 (MacBook Air) 通过 SSH 隧道 localhost:28789 → 205:18789 访问 OpenClaw Gateway
- 昨天手动建的隧道过夜断了，没有自动恢复机制

### 排查过程
- 205 上有个无用的自连隧道 (ssh -L 28789 到自己) + com.autossh.openclaw LaunchAgent，已清理
- 152 上通过 SSH 远程 `launchctl bootstrap gui/501` 不可靠（macOS 26 限制），job 注册但 runs=0
- `launchctl kickstart` 可以强制启动
- 手动 SSH 隧道完全正常，免密双向已配好 (205↔152)

### 最终方案
1. **隧道脚本**: 152 上 `~/openclaw-tunnel.sh` — while true 循环 + ServerAliveInterval=10 + 断线 5 秒重连
2. **LaunchAgent**: `com.openclaw.tunnel.plist` — RunAtLoad + KeepAlive
3. **启动保障**: `~/.zprofile` 里加了 `launchctl kickstart gui/501/com.openclaw.tunnel`，登录后 10 秒自动 kick
4. **205 监控**: `monitor-152-tunnel.sh` 加入 HEARTBEAT.md，定期检查 + 远程 kickstart
5. **205 清理**: 删除了无用的 com.autossh.openclaw LaunchAgent 和自连隧道

### 配置备忘
- 152 IP: 192.168.0.152
- 205 IP: 192.168.0.205 (Gateway :18789, bind: lan)
- openclaw.json controlUi.allowedOrigins 含 http://localhost:28789（152 用）
- 152 无 brew/autossh，用 wrapper 脚本替代

## 15:41 最小治理闭环 1.0 落地
- 采纳“少做、做准”的治理思路，不做大而全重构
- 新增 `WORKING_RULES.md`：固定 5 条规则（长期信息落盘、主会话分流、四层排障、fallback 仅作保险丝、重复故障入病历）
- 新增 `INCIDENT_LOG.md`：建立极简病历系统，并录入 3 个近期高价值故障样本
- 当前原则：先用最小制度提升稳定性，不扩文件体系，不扩角色编制
- 新增 `SESSION_USAGE_CARD.md`：将主会话 / 调研 / 修复 三类 session 的边界压成一页操作卡，降低后续使用歧义

## 16:31 webchat approval 链路故障应急处置
- 现象：control-ui / webchat 中无论新旧 approval id，提交 `/approve ... allow-once` 均返回 `GatewayClientRequestError: unknown or expired approval id`
- 判断：不是用户操作错误，而是 approval 提交通道本身故障，导致所有需要 elevated approval 的排障都被卡死
- 已先在 `~/.openclaw/openclaw.json` 中关闭 webchat 相关 elevated（agent main + global tools），并由 Bruce 手动 `openclaw gateway restart` 生效
- 代码定位：3-key 轮换主循环仍会在 failover 前后产生前端可见错误/草稿生命周期问题；已确认 `auth-profiles` 当前为 MRU 排序、custom-zenmux-ai 未在 cooldown bypass 列表内，说明重复 402 不是旧 bypass 逻辑残留
- 16:36 Bruce 要求先止血：撤销 3-key 轮换，恢复单 key 正常模式，避免继续影响工作
- 初次回退时误将 `auth.order` 写成数组，导致 `openclaw.json` 校验失败，连带影响所有入口；已立即撤销该错误配置
- 已将 `auth.profiles` 中 `custom-zenmux-ai:key2/key3` 删除，仅保留 `key1`
- 为避免继续被 zenmux 402/重试链路影响，曾临时将 agents 默认主模型切到 `openai-codex/gpt-5.4`
- 16:53 Bruce 明确要求改为单线直连 `key1`，不用当前 fallback 主链；已将默认主模型切回 `custom-zenmux-ai/anthropic/claude-opus-4.6`，仅保留 `openai-codex/gpt-5.4` 作为配置 fallback
- 16:55 Bruce 反馈 `key1` 已无额度，要求改为 `key2` 单线直连；已将 auth 中唯一保留的 zenmux profile 改为 `custom-zenmux-ai:key2`
- 16:58 key2 仍报 402，Bruce 要求切到 `key3` 单线直连；已将 auth 中唯一保留的 zenmux profile 改为 `custom-zenmux-ai:key3`
- Bruce 随后确认 key1/key2/key3 手测均为 402，问题已定性为 key 全部无额度，不再继续在 zenmux 主链上浪费时间
- 17:05 按 Bruce 要求，将默认主线路切回稳定可用的 `openai-codex/gpt-5.4`；fallback 改为 `openrouter/anthropic/claude-sonnet-4-20250514`
- 22:10 Bruce 决定重新把默认主模型切到 zenmux `key1`；已将 auth 中唯一保留的 zenmux profile 改回 `custom-zenmux-ai:key1`，并将默认主模型改回 `custom-zenmux-ai/anthropic/claude-opus-4.6`
- 下一步：Bruce 手动 `openclaw gateway restart`，让 key1 主线路生效

## 22:23 OpenClaw 升级 2026.3.23-2 → 2026.3.24

- `npm update -g openclaw` 完成
- 新版文件名：`auth-profiles-XJahKVCp.js` / `pi-embedded-BaSvmUpW.js`
- 三处 patch 新版均未原生修复，已全部重打：
  1. auth-profiles L1143: lastUsed 排序改降序（MRU sticky）
  2. pi-embedded L3787: RAW_402_MARKER_RE 末尾补 `^\s*402\s*[\{\[]`
  3. cooldown bypass: 新版未含 custom-zenmux-ai，无需改动
- 旧文件备份在 `workspace/patch-backup/`
- 待 Bruce 执行 `openclaw gateway restart` 生效

- wecom 插件升级 2026.3.20 → 2026.3.26（手动 npm pack + 解压，因 `openclaw plugins update` 未拉新版）
- 重启后 wecom WebSocket 短暂断连，自动重连成功
- 全部完成确认 ✅

## 22:50-23:40 WORKING_RULES.md 系统思考规则体系优化

### 起因
- Bruce 收到外部建议（系统性评估+变更管理），要求评估是否需要采纳
- 评估结论：方向正确但已有覆盖，不额外加文件

### 改动清单
1. **WORKING_RULES.md 全面重构**：
   - 新增"根基：系统思考"段（五个维度从"看XX"改为具体问题）
   - 新增"快捷口令"表（Bruce说关键词→我执行对应动作）
   - 规则3从9个检查点压缩为5步流程（①→⑤）
   - 新增规则4（多路径比较、全局最优）
   - 原规则4→5，原规则5→6
   - 全文从~80行压缩至47行，去除重复和废指针
2. **INCIDENT_LOG.md**：新增"变更模板"段 + 补录近两天3条关键变更
3. **AGENTS.md**：Session Startup 第4步加入读 WORKING_RULES.md
4. **WORKING_RULES.md 回滚预案**：规则3③备份再动手

### 关键决策
- 治理文件归 main 维护，不同步到 chief/chief-user（symlink 共享的是业务文件）
- 不新建"变更管理"文件，变更记录并入 INCIDENT_LOG.md
- 规则文件必须自包含可执行，不用废指针指向非必读文件

## 00:16 WORKING_RULES 实战验证 + monkey-patch 自动化

### 规则实战
- 用 monkey-patch 问题做了完整的规则执行演示（五个维度→规则3→规则4）
- 规则4判定：路径B（自动化脚本）+ 路径C（上游issue）组合为全局最优
- 熔断规则起了作用：已手动打2次，接近3次熔断线

### 执行
- 上游 issue 已提：openclaw/openclaw#55897（MRU sticky + silent 402 failover）
- `patch-openclaw.sh` 脚本完成并测试通过：
  - Patch 1：auth-profiles MRU sticky sort — ✅ 能打、能检测已打
  - Patch 2：pi-embedded 402 regex 扩展 — ✅ 能检测已打
  - 自动备份到 patch-backup/
  - 升级后一条命令：`bash ~/.openclaw/workspace/patch-openclaw.sh`

## 2026-03-28 01:21 key 切换
- 按 Bruce 要求，将 `~/.openclaw/openclaw.json` 中唯一保留的 zenmux 配置从 `custom-zenmux-ai:key1` 切到 `custom-zenmux-ai:key2`
- 当前为配置已切换，**需执行 `openclaw gateway restart` 才会生效**
