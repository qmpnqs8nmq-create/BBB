## 默认大模型顺序调整
- 按 Bruce 要求，将全局默认链改为 `openai/gpt-5.6-sol` → ZenMux key1/Fable 5 → ZenMux key2/Fable 5。
- `api-worker`、`market` 保留原 agent 级覆盖；其余未覆盖 agent 继承新默认。
- dry-run、配置回读、模型列表及 Gateway probe 均正常；热加载，无重启。
- 回滚：`/root/.openclaw/backups/openclaw.json.pre-gpt56sol-default-20260901`。

## 删除客服 agent
- 按 Bruce 明确要求永久删除 `kefu`，不创建备份；删除前确认无 binding、无专属 cron、无运行进程/容器。
- OpenClaw 删除注册项、workspace 与 agent 状态/会话；另清理旧备份中的客服子目录及 370 个客服 Wiki bridge source。
- 共享巡检 cron 精准移除客服路径，其他 agent 检查不变；旧共享配置备份中仅移除 `kefu` agent 对象。
- Wiki 已重新 compile/lint；agent、binding、cron、路径及 Gateway probe 复核正常。删除不可恢复。

## 删除后系统只读审计
- `healthcheck` 深审计：0 critical / 4 warn / 1 info；Gateway runtime/probe 正常，客服无 agent/binding/cron/路径/Wiki 残留，无断裂 workspace symlink。
- 首要运行问题：8 个 cron 连续失败 4–13 次；日志显示 OpenAI 路由命中 inactive billing，ZenMux Fable 5 又报 subscription 不含该模型，当前后台模型链不可靠。
- 系统债务：407 个 chief orphan transcripts（sessions 总目录 326M）；OpenClaw 2026.8.1 可升级；78 个 apt 更新、其中 26 个 security，且系统提示需重启。
- 安全姿态：UFW 入站默认拒绝，仅放行 22/80/443；18789 虽 bind 0.0.0.0，但未在 UFW 放行，由 nginx:443 反代。SSH 禁密码、root 仅密钥；TLS 为 IP 自签证书。
- 其他告警：openclaw.json 有明文 token/API keys、11 个 agent 配置 exec security=full、find/sed allowlist 缺 strictInlineEval、Codex npm spec 未 pin、飞书建文档可给请求人授权。仅审计，未修改。
