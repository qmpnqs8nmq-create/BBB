## 08:00 每日自检
- Gateway 运行正常、连通性探测通过；最近 50 行日志无网关异常。
- 6 个 cron 处于错误状态：3 个微信投递 `prepare failed`（连续 14/13/1 次），3 个历史 `LLM request failed`（连续 5 次）；当前主模型调用正常，未做高风险配置修改。
- workspace 与 workspace-chief 已分别自动提交并尝试推送：`64a378f`、`149f5cc`。

## 08:18 heartbeat 维护
- 将满 14 天的 `2026-08-19.md` 提炼为 1 行写入 `memory/archive.md`，原文件已移入系统回收站。
- `MEMORY.md` 78 行，无需精简；archive 无超过 6 个月段落；近 48h 未发现 WeCom DM 拒绝/拦截。

## 09:40 OpenClaw 2026.8.2 升级后只读审计
- CLI、npm 全局包、Gateway runtime 均为 `2026.8.2`；stable latest 同为 8.2，systemd active/probe ok，仅单一 Gateway PID 1905768。
- 阻塞故障：个人微信 2.4.6 在 8.2 下导入 `plugin-sdk/channel-runtime` 失败，5 个账号反复自启后 not-running；patched channel.js 本体与备份 hash 一致，但插件兼容链断裂。
- 402 自愈脚本仍只扫描 `errors-*.js`，8.2 已迁移到 `classify-*.js`，启动日志明确 `no file found`；当前引号值 402 正则补丁未生效。
- Feishu/Perplexity 仍为 2026.7.1，Gateway 报 2 个官方插件 drift；Codex 已为 2026.8.2。插件 registry 另有 stale-source warning。
- 企业微信鉴权成功、飞书 WebSocket 启动；安全审计仍为 0 critical / 4 warn / 1 info，UFW 与外露端口姿态未变。
- Gateway 启动后出现 memory pressure，cgroup peak 5.67G、后续 current 升至 5.16G；5 个微信账号已累计 40 次失败并继续重试，修复后必须复测。
- 81M 回滚包全部 gzip 校验通过，目录 700、敏感文件 600；根盘 68%/余 16G。OS 仍提示需重启。
- npm latest 微信插件 2.4.8 已改用 8.2 新 export，具备兼容修复方向，但不含 8/14 本地 warm-up/ret=-2 补丁，更新时必须重新移植并验证。
- 全程只读，未改配置/插件/服务；下一步需 Bruce 批准插件升级、402 脚本适配与一次 Gateway restart。

## 10:06 OpenClaw 2026.8.2 集中修复完成
- Bruce 明确批准后创建修复前快照 `/root/.openclaw/backups/openclaw-2026.8.2-repair-pre-20260902-094821`；tgz 可解压、文件权限 600/目录 700。
- 微信升级并 pin 至 2.4.8，Feishu/Perplexity pin 至 2026.8.2；微信 warm-up/ret=-2 本地补丁已移植且语法检查通过，12/12 插件兼容检查通过。
- 402 自愈脚本改为按 `RAW_402_MARKER_RE` 扫描所有顶层 bundle，8.2 的 `classify-*` 已命中；零候选/形状变化返回非零，重复执行幂等。
- 为 `contact-delivery` 补 `send_contact` 工具契约；为现有 WeCom 2026.5.7 增加 `hooks.allowConversationAccess=true`，未冒险替换整套企业微信技能。
- systemd 实际重启于 09:56:43 完成；旧 Codex turn 因进程终止显示 `client closed`，新 PID 1913334 active/probe ok，CLI/Gateway 均 2026.8.2。
- 飞书/企业微信 probe 均 `works`，5 个个人微信账号全 running，且重启后个人微信真实消息处理成功；主记忆索引自行恢复为 Dirty=no、FTS ready。
- cgroup 首次数据库校验短时峰值 4.07G，校验 worker 退出后回落至约 1.1G，未再出现 memory pressure；安全审计仍为 0 critical / 4 warn / 1 info。
- 既有非阻塞项保留：Codex 安装 spec 未 pin、doctor 的工具策略提示、OS 仍要求宿主机重启；未扩大权限或调整全局模型配置。

## 10:15 集中修复后遗留项只读复核
- OpenClaw/Gateway 仍为 2026.8.2，12/12 插件与三类通道探测通过；NRestarts=0、无 OOM/通道重启，安全审计仍为 0 critical / 4 warn / 1 info。
- 宿主机明确因 `libc6` 更新等待 OS reboot；需另约维护窗口，重启后复测 Gateway/通道/防火墙。
- 模型链有两项真实债务：`google/gemini-3.1-flash-image-preview` 未在 provider 注册；ZenMux key1/Fable 5 返回订阅计划 404，key2 fallback 成功。
- `openclaw health` 另报 46 条历史 dead-letter（最老 99 天）；应先审查再清理，不影响当前通道。
- Gateway 在本轮 doctor/多 Codex app-server 活跃时约 2.7G、峰值 4.4G，累计 3 次 memory-pressure 警告但无 OOM；建议观察空闲期趋势，持续高于 3–4G 再专项排查。

## 10:32 升级后第二批维护（重启前）
- Bruce 已批准备份、模型链修复、Codex pin、宿主机 reboot 与复测；healthcheck 流程执行。
- 精确备份完成：verified 配置归档、28M 全局 SQLite 官方快照、Codex/systemd 元数据，均位于 `/root/openclaw-backups/openclaw-post-8.2-*-pre-20260902-101915*`。
- 全状态备份临时占用 4.9G，因磁盘风险主动终止并删除仅含未完成快照的临时目录；根盘恢复 67%/余 16G。
- Google provider 补官方 `google-generative-ai` + v1beta endpoint；真实独立请求 HTTP 200/MODEL_OK。
- 默认 fallback 删除订阅 404 的 key1，仅保留 key2/Fable 5；key2 真实独立请求 HTTP 200/FALLBACK_OK。
- Codex install.spec/resolvedSpec 已 pin 为 `@openclaw/codex@2026.8.2`，integrity 不变；pre-reboot config valid、12/12 plugins doctor、Gateway probe ok。
- reboot 前 boot_id `f490d9ac-74c5-4e2c-a878-9fd2306134d2`；one-shot 接续 job `0fd51ea8-1fb8-4931-a24d-5c7b0366d994` 将在启动后复测。

## 10:55 宿主机重启复测与 OpenAI 迁移评估
- 宿主机 boot_id 已变为 `69b59229-f859-4c44-9cd3-77ad34861c26`，`reboot-required` 清除；Gateway 2026.8.2 PID 2077，active/probe ok、NRestarts=0。
- 12/12 插件兼容；Feishu admin ready、WeCom running、5 个 Weixin 账号 running；Google 与 key2 独立请求分别返回 `POST_REBOOT_GOOGLE_OK` / `POST_REBOOT_FALLBACK_OK`。
- 安全审计为 0 critical / 3 warn / 1 info；Codex pin 告警已消失。UFW 仍仅放行 22/80/443，18789 未直接放行。
- cgroup 约 11.7G 中约 10.1G 为可回收 file cache，anon 约 1.22G；Gateway RSS 约 956M、event loop healthy，无 OOM/重启。
- `gemini-3.1-flash-image-preview` 唯一活动用途是纯文本 Validation Tracker，不是图像生成；系统默认 imageModel 本来就是 `openai/gpt-5.5`。
- OpenAI 迁移实测均失败：chief 的 `openai/gpt-5.6-sol` 无 usable profile，`openai/gpt-5.5` inactive account，`openai-codex/gpt-5.5` 与 OpenRouter/OpenAI 均 HTTP 403。
- 为避免周三任务停摆，cron、provider 登记与 allowlist 已完整回滚到已验证成功的 Gemini；临时候选项已清除。迁移需先补齐后台 agent 可用的受保护 OpenAI 认证。
- Secret Store 当前为空；已发起 `OPENAI_API_KEY` 掩码录入（仅允许 api.openai.com），本轮未收到凭据，未在聊天或配置中收集明文。

## 14:58 OpenClaw 8.2 最终升级后审计闭环
- 完成精确回滚包 `/root/.openclaw/backups/openclaw-8.2-final-audit-pre-20260902-1400` 全量 SHA-256 校验；9 个 agent SQLite 压缩快照和配置/补丁/systemd 文件全部通过。
- Ubuntu 61 个常规更新已安装，fwupd 版本错位修复；AppArmor 更新后整机重启成功，Node 24.20.0，failed units=0、reboot-required=no，根盘 70%。
- SSH 维持密钥登录并收紧 `MaxAuthTries=3`、关闭 X11；UFW 仅开放 22/80/443，nginx 配置通过，18789 未被防火墙放行。
- Gateway/CLI/stable 均 2026.8.2；插件 doctor、三通道 probe、event loop、402/微信 CLI metadata 持久化门禁通过，个人微信外发实测 delivered=true；测试 job 已删除。
- 11 个 agent memory identity 全部 valid，FTS/vector/embedding probe 全通过；main/chief/mangba dirty 仅因活动会话写入。主模型和 mangba 实测返回 `MAIN_MODEL_OK`/`MANGBA_MODEL_OK`。
- 安全审计降至 0 critical / 2 warn / 1 info；剩余 warn 为已知信任边界（exec=full、Feishu doc owner grant），未为清零提示破坏现有 agent 能力。
- 46 条历史 outbound dead-letter 无 8.2 官方安全清理接口，未直接改状态库；Secret audit 为 128 plaintext/23 legacy、0 unresolved，文件权限已是目录700/文件600，迁移需后续掩码录入。
- Registry `source-changed` 定位为 persisted workspace=main/current workspace=chief 的 8.2 多 workspace 判定问题，实际 differences=[]、doctor/运行时正常；refresh 仅临时 fresh，未补丁核心。
- Validation Tracker 的 chief OpenAI 隔离 cron 实测实际转到 ZenMux fallback，故保持已验证 Gemini；临时测试 job 已删除。heartbeat 超时来自本轮长 main turn 占用同一 agent lane，13:49 前连续成功，配置不改。

## 15:18 heartbeat 维护
- 无满 14 天的 daily log；`MEMORY.md` 78 行，无需精简。
- `archive.md` 删除已超过 6 个月保留窗口的 2026-03 月段（可从 git 历史恢复）。
- 近 48h 未发现 WeCom DM 白名单拒绝/拦截；日志命中仅为已知工具 allowlist/插件 hook 告警。

