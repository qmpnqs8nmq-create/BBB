# 2026-03-19

- 08:00 每日自检：`openclaw status --deep`、`openclaw security audit --deep`、`openclaw update status` 均可运行；Gateway LaunchAgent 正在运行，版本/更新状态正常。
- 本次发现一个延续性治理问题：CLI / deep probe 连接本机 Gateway 成功，但仍提示 `missing scope: operator.read`，导致 `openclaw gateway probe` / `status --all` 的部分诊断信息受限；这不影响当前 cron/tool 基本工作，但说明某个本地/已配对身份的 scope 基线仍不完整，后续需定向排查设备授权或凭据来源。
- 其他结果：安全审计无 critical；保留告警仍包括 `gateway.trustedProxies` 未配置，以及 Feishu doc create 权限面较宽。
