# REM Sleep

### Reflections
- Theme: `gateway` kept surfacing across 39 memories.
  - confidence: 1.00
  - evidence: memory/2026-07-11.md:5-8, memory/2026-07-12.md:11-14, memory/2026-07-12.md:18-21
  - note: reflection

### Possible Lasting Truths
- - 根因：hybrid `candidateMultiplier=4`，向量/关键词候选并集再进入同步 MMR；MMR 实现完整重排全部候选，三重循环式增长并阻塞 Node 事件循环。maxResults=20 时候选最多约160条，15s timer 也被同步计算延迟，故37–47s后才返回“15s超时”。 - 临时配置 A/B：保持 maxResults=20，仅关闭 MMR=6.2s；保持 MMR、candidateMultiplier 4→1=6.5s。生产配置尚未改；建议仅 benben 关闭 MMR，保留候选召回与相关性排序。 ## 14:39 会话交接状态 - 当前停在变更授权前：生产配置未修改，等待 Bruce 确认“仅对 benben 关闭 MMR”。 - 获确认后的执行顺序：备份配置 → benben-only 关闭 MMR → 不重启 Gateway（优先热加载）→ 用 benben 运行态、`maxResults=20` 连续真实检索 3 次，记录耗时与结果。 - 验收目标：3 次均在 15 秒内成功，无 `disabled:true`、无 60 秒冷却、无误导性 embedding/provider 报错；失败则回滚并继续定位。 ## 14:49 benben MMR 性能修复完成 - Bruce 确认后，备份 `/root/.openclaw/openclaw.json` 至 `/root/.openclaw/backups/benben-mmr-disable-20260720-1443/`，只为 benben 增加 `memorySear [confidence=0.71 evidence=memory/2026-07-20.md:44-55]
- - `dirty=true` 仍存在，但 scan=301/301、issues=[] 且实时检索成功，判断为活动 session 持续写入产生的瞬时脏标记，不是索引身份故障；未重启 Gateway，运行态健康。 ## 14:35 本本 memory_search 重建后仍超时的完整根因 - Bruce 反馈重建后本本仍报 `memory_search timed out after 15s`；trajectory 确认 14:17:11 发起、14:17:48 返回，属于新故障，不能与旧 providerKey mismatch 混同。 - 分层单测：Gemini 同模型/同长查询直连 0.45–0.67s；OpenClaw SSRF HTTP 包装约 0.5s；sqlite-vec KNN(k=640)约 0.95s；凭据哈希 root/benben 均为 `47eefbaaae16b760`。排除 provider、网络、密钥与数据库损坏。 - 阶梯基准：maxResults=5 → 6.6s，10 → 13.5s，20 → 46.6s。当前 memory tool 源码硬截止 15s、失败后同 agent 冷却 60s；错误包装会误报 embedding/provider。 - 根因：hybrid `candidateMultiplier=4`，向量/关键词候选并集再进入同步 MMR；MMR 实现完整重排全部候选，三重循环式增长并阻塞 Node 事件循环。maxResults=20 时候选最多约160条，15s timer 也被同步计算延迟，故37–47s后才返回“ [confidence=0.66 evidence=memory/2026-07-20.md:37-44]
- - 结论不是索引复坏或 memory handler 随机不稳：benben CLI 当前实时检索成功；原父线程四次分主题查询的 Gateway trajectory 均 `success=true`。 - 父线程的“4 组空结果”是解析错误：`functions.exec` 内动态工具返回 JSON 字符串，代码却读 `r.results`，因此正常结果被压成 `[]`。 - 两个 Codex 原生子代理的 handler 报错是真实的桥接缺口：活动 Codex `2026.7.1-1` handler 仅匹配父线程精确 `threadId + turnId`；子线程继承了工具目录但请求无人处理，落入 app-server 默认错误。 - 当前规避：动态检索由父线程执行并把结果交给子代理；返回值先 JSON.parse 且错误 fail closed。未改配置、索引或 Gateway；已写 INCIDENT_LOG。 [confidence=0.63 evidence=memory/2026-07-20.md:70-74]
