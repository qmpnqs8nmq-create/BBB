# 2026-04-17

## 10:35 Lilian 微信路由验证通过

- Bruce 反馈已测试通过，Lilian 现在可以正常接入 benben
- 昨晚修复闭环完成：binding 改为真实账号 `e1263c708c9c-im-bot`，旧 mangba 误入会话已清理

## 15:56 OpenClaw 系统审计（只审不改）

- 范围：系统本体（Gateway / 配置 / auth / 暴露面 / 日志），不含业务
- 结果：🔴 4 / 🟡 6 / 🟢 5
- 三大高危：
  1. mangba 与 mangba-guest auth-profiles.json 空（37B），与 4-16 benben 事故同根因——未触发因为没流量
  2. Gateway :18789 bind=0.0.0.0 + nginx 443 反代到公网 43.106.3.174，仅靠 token 防护
  3. sub-agent spawn 被拒 `model not allowed: zenmux-key1/anthropic/claude-opus-4.7`，该 id 就是默认 primary
- 今日 log 中 7 次 `Unknown model: anthropic/claude-opus-4.7`（裸 id 走到 provider），与 #3 是同一问题两面
- 完整报告：→ task: memory/tasks/openclaw-system-audit-2026-04-17.md
- 等 Bruce 指示哪些可以动手修

## 16:10 审计修复执行

- 已修：
  - mangba / mangba-guest auth-profiles 同步自 main（5577B/7 profiles）
  - agents/_backup-models-20260329 + agents/claude/ 移到 /root/.openclaw/_attic/2026-04-17/
  - gateway.auth.allowTailscale=false（和 tailscale.mode=off 对齐）
  - agent 目录权限统一 700（api-worker/benben/mangba/mangba-guest）
  - **sub-agent allowlist bug**：清空 `agents.defaults.models={}` → allowAny=true；实测 sub-agent spawn `zenmux-key1/anthropic/claude-opus-4.7` 返回 modelApplied:true ✅
    - 根因：`buildAllowedModelSet` 把 `agents.defaults.models` 当 allowlist，之前里面只有 1 项 `openai-codex/gpt-5.4`，sub-agent 路径的 defaultKey 不命中就 reject
    - 同时理论上修好了日志里 7 次 `Unknown model: anthropic/claude-opus-4.7`
- 未改（Bruce 拍板项）：
  - Gateway `bind=loopback` — Bruce 决定维持现状（token-only 够用）
  - chief `tools.deny` 补兜底
  - memory-core dreaming plugin 显式 enabled（和人工记忆纪律可能冲突）
  - main 目录权限 700（避免踩当前会话）
- 备份：`.bak-modelallowlist-20260417-160950` / `.bak-20260417-160621` / agents/*/auth-profiles.json.bak-20260417-160554
