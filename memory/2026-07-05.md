## 08:00 Daily self-check
- Gateway healthy: runtime active, probe ok, admin-capable.
- Recent logs had state-migration/plugin config warnings but no gateway outage.
- Cron failures flagged: coach audit path bug, Sunday system patrol timeout, two future NVDA reminder quota errors; disabled historical failures also present.
- Safe repair done: fixed coach audit SYSTEM_ITERATION_LOG path, raised Sunday patrol timeout to 420s, ran `openclaw doctor --fix` which tightened `~/.openclaw` perms, cleared stale Anthropic routing state, and normalized cron store.
- Auto snapshots committed for workspace (`6f4661f`) and workspace-chief (`3d36e2b`); push attempted best-effort.
