## 08:00 Daily self-check
- Gateway healthy: runtime active, probe ok, admin-capable.
- Recent logs had state-migration/plugin config warnings but no gateway outage.
- Cron failures flagged: daily self-check prior timeout, coach audit path bug, Sunday system patrol timeout, and two future NVDA reminder quota errors.
- Safe repair done: ran `openclaw doctor --fix` (cron store normalized), set Sunday patrol `lightContext=true` + timeout 600s, and reinforced coach audit path with `lightContext=true` + timeout 360s.
- Auto snapshot step ran for workspace and workspace-chief; no new tracked changes were pending at that moment, push attempted best-effort.
