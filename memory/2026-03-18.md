# 2026-03-18

- Bruce 要求：从今天起每天上午 8 点进行一次自检；若发现问题，先在安全范围内尝试自我修复，再向 Bruce 简要汇报；若一切正常则不打扰。
- 已为 Bruce 创建每天上午 8 点的自动自检 cron；正常时不打扰，异常或有修复时再汇报。
- 今日完成 OpenClaw 安全自查：修复了 `~/.openclaw/openclaw.json` 中旧版 `agents.list[*].systemPrompt` 无效字段，`openclaw status --deep` / `openclaw security audit --deep` / `openclaw update status` 恢复可用。
- 当前 Gateway 已改为 macOS LaunchAgent 后台运行；常用命令是 `openclaw gateway status|start|stop|restart`，不再需要手动前台盯 `openclaw gateway`。
- 已确认 8888 是自定义局域网 HTTPS 代理入口（`scripts/https_lan_proxy_8888.js`），转发链路为 `8888 -> 8899 -> 18789`；5000/7000 是 macOS Control Center。
- 已创建 OpenClaw 手工快照与应急修复资料：完整快照脚本、精简快照脚本、`OPENCLAW_EMERGENCY_RECOVERY.md`、`LLM_TIMEOUT_AUTO_FIX.md`，用于不依赖 Dashboard 的终端自救。
- `LLM request timed out` 的处理规则已定为保守策略：先等待/刷新/重试；仅当本地 OpenClaw 明显异常时，才运行 `~/.openclaw/workspace/scripts/openclaw_fix_llm_timeout.sh` 做自动判断和必要修复。
- 为 8888 局域网浏览器入口新增了本机后台 watcher：`ai.openclaw.watch-8888-pairing-autoapprove`。它不再做定时轮询，而是监听 gateway 日志，遇到 8888 / pairing / scope 相关事件时自动处理。
- 已定位并修复 8888 浏览器入口的关键问题：不是单纯 pending pairing 没批，而是 `openclaw-control-ui` / `webchat` 浏览器设备被批准后缺少 `operator.read` / `operator.write`。现已修正 `~/.openclaw/devices/paired.json` 中这类设备的 approvedScopes 和 token scopes，并升级 watcher 使其今后自动补齐该基线。
- 之后又确认：仅靠日志触发的 watcher 不够可靠；已重构 `scripts/watch_8888_pairing_autoapprove.sh` 为直接读取 `~/.openclaw/devices/pending.json` / `paired.json` 的状态驱动守护，形成“检测 pending → approve --latest → 校验 paired 状态变化 → 自动补 scope → 写日志/状态快照”的闭环，并产出 `PAIRING_8888_RUNBOOK.md` 作为排障与核查标准。
- Bruce 明确要求：不要再把 `8888` 误说成 `888`，包括推理中途和最终输出都不允许；之后凡涉及局域网入口必须始终使用精确端口 `8888`。
- 已确认 chief 侧存在 `workspace-chief/CTX-CONTROL-RULES.md`，其中规定 60%–70% context 应准备切新会话、70%+ 或 UI 提示高 context 时应切会话并做 handoff；但 main / 当前 webchat 尚未接入对应自动执行机制，这被识别为后续要补的治理缺口。
