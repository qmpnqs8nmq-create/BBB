# 2026-04-02

## 15:30 系统升级后全面审计

**版本：** openclaw 2026.4.1 (da64a97)，CLI + Gateway 一致

**审计 10 项全部通过：**
- Gateway 运行正常，三渠道（企业微信/飞书/微信）连通 ✅
- openclaw.json 合法、权限 600 ✅
- Workspace 8 核心文件 + 5 workspace symlink 全部完整 ✅
- 企业微信白名单 8 人 ✅，磁盘 16%、内存 1.2G/3.4G ✅

**关键发现：**
- systemd service 是 user-level（~/.config/systemd/user/），非 system-level
- Linger=yes 已启用，重启后会自动拉起，无需迁移到 system-level
- `systemctl status openclaw-gateway` 查不到是因为要用 `systemctl --user status openclaw-gateway`
- Linux crontab 为空是正常的，openclaw cron 由内置系统管理（23 个 job）
- 安全审计 0 critical、4 warn（飞书 doc create 权限、strictInlineEval、market agent policy 宽松、weixin 插件未锁版本）
# 2026-04-02

## 15:30 系统升级后全面审计

**版本：** openclaw 2026.4.1 (da64a97)，CLI + Gateway 一致

**审计 10 项全部通过：**
- Gateway 运行正常，三渠道（企业微信/飞书/微信）连通 ✅
- openclaw.json 合法、权限 600 ✅
- Workspace 8 核心文件 + 5 workspace symlink 全部完整 ✅
- 企业微信白名单 8 人 ✅，磁盘 16%、内存 1.2G/3.4G ✅

**关键发现：**
- systemd service 是 user-level（~/.config/systemd/user/），非 system-level
- Linger=yes 已启用，重启后会自动拉起，无需迁移到 system-level
- `systemctl status openclaw-gateway` 查不到是因为要用 `systemctl --user status openclaw-gateway`
- Linux crontab 为空是正常的，openclaw cron 由内置系统管理（23 个 job）
- 安全审计 0 critical、4 warn（飞书 doc create 权限、strictInlineEval、market agent policy 宽松、weixin 插件未锁版本）

## 15:40 Control UI 设置保存报错

- 用户在 openclaw-control-ui 修改设置时，截图显示保存失败
- 初步判断：chief agent 的 exec approvals allowlist 条目中含旧字段 `source`，当前 schema 不接受，报 `unexpected property 'source'`
- 错误路径指向 `/file/agents/chief/allowlist/...`，说明是数组中若干旧条目格式不兼容，而非整页配置损坏
- 下一步待做：读取当前配置，定位 chief/allowlist 中含 `source` 的具体条目，删除或迁移不兼容字段后验证 UI 保存
- 该问题大概率与升级后 schema 变严格、旧配置未完全迁移有关
