# 2026-03-31

## 00:02 专家观点评测
- 观测面（全量入库统一事件模型）→ 建设期过度工程化，等稳定后再评估
- 记忆面（转录/事实/画像分离）→ 我们已有三层（transcript / memory files / SOUL+USER），不需要额外建设
- 安全面（零信任）→ 最有价值，暴露了实际差距

## 00:18 Agent 权限审计 + sandbox 加固
**审计发现：**
- chief-user sandbox off，面向全公司最大攻击面无隔离
- chief-user 通过 sessions_spawn → 子 agent（strategy/execution/coach）绕过 write deny
- main 有不需要的 wecom_mcp 权限
- MEMORY.md "Docker Desktop 已安装可用"是错误信息，实际未安装

**执行：**
1. 安装 docker.io 28.2.2，systemd 启动
2. chief-user sandbox: `mode: "all"`, `workspaceAccess: "ro"`, `scope: "session"`
3. strategy/execution/coach deny 加 write, edit, wecom_mcp
4. market deny 加 write, edit
5. main deny 加 wecom_mcp
6. Gateway restart 正常，全渠道在线
7. `openclaw sandbox explain --agent chief-user` 确认 sandboxed

**间接写入攻击链已切断：** chief-user → deny write ✅ → spawn 子 agent → 子 agent deny write ✅ → sandbox 隔离 ✅
