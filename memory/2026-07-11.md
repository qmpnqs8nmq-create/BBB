# 2026-07-11

## 08:00 每日自检 (cron daily-self-check-8am)

- Gateway: running & healthy (pid 2063958, probe ok, v2026.6.10)。
- Cron: 仅 1 个 job，lastStatus=ok，无失败。
- 自动提交：workspace + workspace-chief 均成功 commit（本地）。
- 日志发现两类 ERROR（均非致命/自愈）：
  1. `EmbeddedAttemptSessionTakeoverError` — chief dreaming-narrative 多 lane 在 05:28 并发抢占 session 文件锁；run 最终 stopReason=stop。反复出现，属 dreaming 管线并发竞争，暂无害。
  2. weixin `errcode -14` session expired — 06:05/07:05 各暂停 bot 60min，08:00 已自动恢复 getupdates 轮询。属 WeChat 凭证周期性过期+自愈，无需手动干预（除非要重新 auth）。
- 当前会话走 wecom 通道，不受 weixin -14 影响。
