---
name: preserve-reset-sessions
description: "Keep /new and /reset archived transcripts visible in Sessions list with human-readable labels"
metadata: { "openclaw": { "emoji": "🗂️", "events": ["command:new", "command:reset"] } }
---

# Preserve Reset Sessions

When `/new` or `/reset` archives the current transcript, re-register that archived transcript as a standalone session entry so it remains visible in the Sessions list.
